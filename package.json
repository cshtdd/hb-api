{
  "name": "hb-api",
  "scripts": {
    "create-domain": "sls create_domain --region us-east-1 --stage dev --domainName hbapidev.tddapps.com && sls create_domain --region us-west-2 --stage dev --domainName hbapidev.tddapps.com",
    "int-tests-db": "sh ./dev/start_local_dynamo.sh 8001",
    "predeploy": "npm run int-tests-db",
    "deploy": "mvn clean install && sls deploy --region us-east-1 --stage dev --domainName hbapidev.tddapps.com && sls deploy --region us-west-2 --stage dev --domainName hbapidev.tddapps.com",
    "osa": "mvn clean verify -DskipTests",
    "start": "sh ./dev/run.sh",
    "debug": "sh ./dev/run.sh --debug"
  },
  "dependencies": {
    "serverless": "^1.31.0",
    "serverless-domain-manager": "^2.6.0",
    "serverless-dynamodb-autoscaling": "^0.6.2",
    "serverless-dynamodb-local": "^0.2.33",
    "serverless-sam": "^0.1.0"
  }
}
